 // Sorting using Stream
        List<MyObject> sortedList = myList.stream()
                .sorted((obj1, obj2) -> Integer.compare(obj1.getValue(), obj2.getValue()))
                .collect(Collectors.toList());

        // Print the sorted list
        sortedList.forEach(obj -> System.out.println(obj.getValue()));


printing the duplicate values

SELECT Name, COUNT(Name) AS Occurrences
FROM PersonTable
GROUP BY Name
HAVING COUNT(Name) > 1;

why we use the indexing in the database
CREATE INDEX idx_lastname
ON Persons (LastName);




